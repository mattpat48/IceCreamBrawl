#pragma once

// these components are only used as tags to identify
//entities with certain properties, so they are empty structs

struct isPlayer {};
struct isEnemy {};

struct isTouchable {};
struct isSwipable {};